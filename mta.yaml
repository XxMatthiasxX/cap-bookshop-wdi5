---
_schema-version: '3.1'
ID: I5186464_wdi5_demo
version: 2.0.0
description: "A demo for wdi5 tests in combination with jenkins and piper."
provider: SAP Sample generator # todo delete later?
#parameters:
#  enable-parallel-deployments: true
#build-parameters:
#  before-all:
#    - builder: custom
#      commands:
#        - npx -p @sap/cds-dk cds build --production
modules:
  - name: I518646_piper.node.hello.world
    type: nodejs
    path: .
    parameters:
      buildpack: nodejs_buildpack
#    build-parameters:
#      builder: npm-ci
#    provides:
#      - name: srv-api # required by consumers of CAP services (e.g. approuter)
#        properties:
#          srv-url: ${default-url}
